# Backups

This directory keeps backup artifacts for workspace-level operations.

- `latest.json` is tracked as the canonical latest snapshot pointer.
- Additional backup files should be managed according to your retention policy.
