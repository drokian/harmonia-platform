# How to Sync Demo

Use `scripts/Sync-ToDemo.ps1` to copy safe artifacts to the demo submodule.

## Build Mode Examples
- npm: `-BuildMode`
- custom: `-BuildMode -BuildCommand "pnpm run build" -BuildOutputDir dist`
