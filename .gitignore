# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*~

# Env / secrets
.env
.env.*

# Logs / temp
*.log
*.tmp

# Review artefacts (script çıktıları — local only)
/ai-review/

# AI agent local configs (private — root level only)
/.claude/
/CLAUDE.md
/.github/copilot-instructions.md

# Internal development process (private, root-level only)
/development/
templates/**.md
# Template source files are exceptions — must be tracked
!templates/workspace-with-demo-v1/
!templates/workspace-with-demo-v1/**
